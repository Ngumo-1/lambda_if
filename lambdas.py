def num_square(x):
  return lambda x: x**2

area=lambda l, w: l*w
#function to calculate area of rectangle using length and width

even_number= lambda x: x%2==0
#function for checking if a number is even